name: Scrape Redgate Test Data Manager

on:
  schedule:
    - cron: "40 6 * * *" # daily 06:40 UTC
  workflow_dispatch:

jobs:
  scrape:
    uses: ./.github/workflows/_scrape.yml
    with:
      product: tdm
